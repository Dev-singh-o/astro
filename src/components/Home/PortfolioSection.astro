--- 
const projects = [
  {
    title: "Storm van 's Gravesande",
    url: "advocaatstorm.nl",
    imageSrc: "/images/portfolio1.png",
    imageAlt: "Storm van 's Gravesande project preview",
  },
  {
    title: "SeppR",
    url: "seppr.nl",
    imageSrc: "/images/portfolio2.png",
    imageAlt: "SeppR project preview",
  },
  {
    title: "Storm van 's Gravesande",
    url: "advocaatstorm.nl",
    imageSrc: "/images/portfolio1.png",
    imageAlt: "Storm van 's Gravesande project preview",
  },
  {
    title: "SeppR",
    url: "seppr.nl",
    imageSrc: "/images/portfolio2.png",
    imageAlt: "SeppR project preview",
  },
  {
    title: "Storm van 's Gravesande",
    url: "advocaatstorm.nl",
    imageSrc: "/images/portfolio1.png",
    imageAlt: "Storm van 's Gravesande project preview",
  },
];
---

<!-- Portfolio Section Component -->
<section class="bg-white py-16 lg:py-24">
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <!-- Section Header -->
    <div class="mb-16 text-center">
      <div class="mb-2 text-lg font-medium text-[#2980B9]">Laatste projecten</div>
      <h2 class="text-3xl font-bold text-gray-900 md:text-4xl lg:text-5xl">
        Nieuw werk waar we trots op zijn
      </h2>
    </div>

    <!-- Projects Horizontal Scroll -->
    <div class="relative">
      <!-- Left Arrow -->
      <button
        id="portfolio-left"
        class="absolute left-0 top-1/2 z-10 flex h-12 w-12 -translate-y-1/2 items-center justify-center rounded-full border border-[#2980B9] bg-white/60 text-[#2980B9] shadow-xl backdrop-blur-md transition-all duration-300 hover:bg-white/80 hover:scale-105"
        aria-label="Scroll left"
      >
        <svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
        </svg>
      </button>

      <!-- Cards Container -->
      <div
        id="portfolio-scroll"
        class="flex overflow-x-hidden scroll-smooth space-x-8 py-10 px-16"
      >
        {projects.map((project, index) => (
          <div
            key={`${project.title}-${index}`}
            class="min-w-[440px] max-w-[440px] h-[360px] flex-shrink-0 rounded-2xl bg-white shadow-lg overflow-hidden transition-shadow duration-300 hover:shadow-xl group"
          >
            <!-- Image Placeholder -->
            <div class="aspect-video bg-gradient-to-br from-blue-100 to-blue-200 relative overflow-hidden">
              <div
                class={`absolute inset-0 flex items-center justify-center ${index % 2 === 0 ? 'bg-[#D4D4D8]' : 'bg-[#EC6A2C40]'}`}
              >
                <div class="p-4 pt-16 text-center">
                  <img
                    src={project.imageSrc}
                    alt={project.imageAlt}
                    class="h-auto w-full object-contain"
                  />
                </div>
              </div>
            </div>

            <!-- Card Content -->
            <div class="p-6">
              <div class="flex items-start justify-between">
                <div class="flex-1">
                  <h3 class="mb-2 text-xl font-bold text-gray-900">{project.title}</h3>
                  <div class="mb-3 flex items-center text-sm text-gray-500">
                    <img
                      src="/images/icons/glob.png"
                      alt={`${project.title} website icon`}
                      class="h-4 w-4 object-contain pr-1"
                    />
                    {project.url}
                  </div>
                </div>

                <!-- Arrow -->
                <div class="ml-4 flex-shrink-0">
                  <div class="flex h-10 w-10 items-center justify-center rounded">
                    <img
                      src="/images/icons/arro.png"
                      alt="Pijl icoon"
                      class="h-6 w-5 object-none pr-1"
                    />
                  </div>
                </div>
              </div>
            </div>
          </div>
        ))}
      </div>

      <!-- Right Arrow -->
      <button
        id="portfolio-right"
        class="absolute right-0 top-1/2 flex h-12 w-12 -translate-y-1/2 items-center justify-center rounded-full border border-[#2980B9] bg-white/60 text-[#2980B9] shadow-xl backdrop-blur-md transition-all duration-300 hover:bg-white/80 hover:scale-105"
        aria-label="Scroll right"
      >
        <svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
        </svg>
      </button>
    </div>

    <!-- CTA Button -->
    <div class="mt-12 text-center">
      <a
        href="/projecten"
        class="inline-flex items-center rounded-lg border-2 border-orange-500 px-8 py-4 text-lg font-semibold text-orange-400 transition-all duration-300 transform hover:scale-105 hover:shadow-lg group"
      >
        Bekijk alle projecten
        <img
          src="/images/icons/arroorg.png"
          alt="Pijl icoon"
            class="w-4 h-4 ml-1 object-contain group-hover:translate-x-1 transition-transform duration-200"
        />
      </a>
    </div>
  </div>
</section>

<!-- Portfolio Scroll Script -->
<script>
  document.addEventListener("DOMContentLoaded", () => {
    const scrollContainer = document.getElementById("portfolio-scroll");
    const leftBtn = document.getElementById("portfolio-left");
    const rightBtn = document.getElementById("portfolio-right");
    const cardWidth = 440 + 32; // Card width (440px) + space-x-8 (32px)

    if (!scrollContainer || !leftBtn || !rightBtn) return;

    leftBtn.addEventListener("click", () => {
      scrollContainer.scrollBy({ left: -cardWidth, behavior: "smooth" });
    });
    rightBtn.addEventListener("click", () => {
      scrollContainer.scrollBy({ left: cardWidth, behavior: "smooth" });
    });
  });
</script>